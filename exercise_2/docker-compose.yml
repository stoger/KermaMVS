version: '3.3'
services:
    python:
        build:
          dockerfile: Dockerfile
          context: .
        restart: always
        ports:
            - '18018:18018'
        volumes:
            - peerdb:/peers
        container_name: kerma-python
        image: python

volumes:
    peerdb:
